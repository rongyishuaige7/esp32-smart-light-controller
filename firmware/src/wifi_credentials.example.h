#ifndef SMART_LIGHT_WIFI_CREDENTIALS_EXAMPLE_H
#define SMART_LIGHT_WIFI_CREDENTIALS_EXAMPLE_H

// This public default deliberately contains no network credentials. Copy this
// file to wifi_credentials.h (which is ignored by Git), then enter the
// credentials of a trusted local test network if you need the optional HTTP
// interface. With these values left empty, the firmware never starts Wi-Fi or
// HTTP services.
#define WIFI_SSID ""
#define WIFI_PASSWORD ""

#endif

# 项目状态

> 状态日期：2026-07-18

| 层级 | 状态 | 当前证据 |
| :-- | :-- | :-- |
| 源码来源 | 已确认 | 来源为桌面 `smart_light_controller` 的 5 个允许公开的文本文件；原目录保持只读。 |
| 公开净化 | 已建立 | 候选不复制 `.pio/`、`.vscode/`、构建产物、IDE 状态、媒体或本机配置；页面移除外部字体/CDN；公开默认不含 Wi‑Fi 凭据。 |
| 安全收口 | 已建立 | 公开固件取消启动全亮测试和 HTTP 网络重置；默认不启动 Wi‑Fi/HTTP；输出与自动模式不跨重启恢复；传感器不可用不改变自动输出。 |
| 公开仓门禁 | 已建立 | 脚本检查敏感信息、生成物、结构、BOM/SVG、依赖锁定、源码行为契约和状态文案。 |
| 固件构建 | 待本轮验证 | 公开候选必须在隔离 `scripts/verify.sh` 及 GitHub exact-HEAD CI 中构建；原始目录历史构建不是本条证据。 |
| 当前真机复测 | 未执行 | 当前候选没有按公开 commit 重新烧录或联调 ESP32、LED、BH1750、NVS、本机凭据连接、HTTP、倒计时或自动模式。 |
| 媒体与 EDA | 未提供 | 当前没有公开实物照片、演示视频、原理图、PCB、Gerber 或制造文件。 |

## 权威状态摘要

```text
源码来源已确认
公开候选已完成安全启动、依赖锁定、传感器不可用语义和本地 HTTP 边界整理
固件构建、GitHub Actions 和当前真机复测必须分别以最终公开仓的精确 commit 为准
当前 ESP32、四路低压 LED、BH1750、NVS、本机凭据连接、HTTP、倒计时和自动模式尚未按当前公开提交重新真机复测
```

## 不应扩大的结论

- CI、源码契约、PlatformIO 构建、Artifact、HTTP 返回和页面显示不证明真实 LED 已变化、设备在线、控制必达、传感器准确、网络安全、节能效果或长期稳定性；
- BH1750 的光照值只是当前程序读到的候选数据，不是经校准的照度、节能或环境结论；
- 自动模式是四路联动状态机，不是每路独立策略，也不构成无人值守、可靠或安全控制；
- 本项目不是市电照明、智能家居成品、公共/应急照明、安防、消防、医疗、生产控制或其他安全关键系统。
